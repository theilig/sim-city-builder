export function useRecommendations() {
    const calculateRecommendations = (shoppingLists, storage, running, recommendations, priority) => {
        return {recommendations: {}}
    }
    return {
        calculateRecommendations
    }
}
